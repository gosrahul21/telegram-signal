import { OnModuleInit } from '@nestjs/common';
import { TechnicalAnalysisService } from './technical-analysis.service';
import { PriceMonitoringService } from './price-monitoring.service';
import { NotificationService } from './notification.service';
import { AlertCreatedEvent, AlertDeletedEvent, AlertService, AlertStatusChangedEvent, AlertUpdatedEvent } from '../alert';
export declare class MonitoringService implements OnModuleInit {
    private readonly alertService;
    private readonly technicalAnalysisService;
    private readonly priceMonitoringService;
    private readonly notificationService;
    private readonly logger;
    private activeAlerts;
    private monitoringIntervals;
    constructor(alertService: AlertService, technicalAnalysisService: TechnicalAnalysisService, priceMonitoringService: PriceMonitoringService, notificationService: NotificationService);
    onModuleInit(): void;
    handleAlertCreated(event: AlertCreatedEvent): Promise<void>;
    handleAlertUpdated(event: AlertUpdatedEvent & any): Promise<void>;
    handleAlertDeleted(event: AlertDeletedEvent): Promise<void>;
    handleAlertStatusChanged(event: AlertStatusChangedEvent & any): Promise<void>;
    handleOrderCreated(event: any): Promise<void>;
    handleOrderUpdated(event: any): Promise<void>;
    handleOrderFilled(event: any): Promise<void>;
    performPeriodicChecks(): Promise<void>;
    private loadActiveAlerts;
    private startMonitoringAlert;
    private stopMonitoringAlert;
    private startPriceMonitoring;
    private startBollingerBandsMonitoring;
    private startEMACrossoverMonitoring;
    private startRSIMonitoring;
    private startMACDMonitoring;
    private startMonitoringOrder;
    private updateOrderMonitoring;
    private stopOrderMonitoring;
    private checkAlertConditions;
    private checkPriceCondition;
    private checkBollingerBandsCondition;
    private checkEMACrossoverCondition;
    private checkRSICondition;
    private checkMACDCondition;
    private triggerAlert;
    private getMonitoringInterval;
    private hasSignificantChanges;
    getActiveAlerts(): Promise<any[]>;
    getMonitoringStatus(): Promise<any>;
}

import { RSISignal } from "./renderRSISignal";
export declare const rsiSignal: (currentRSI: number, keyName: string, price: number, time: number, neutral?: boolean) => RSISignal[];
export default rsiSignal;
